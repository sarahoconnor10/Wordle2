<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
             x:Class="Wordle.StatsPopUp">

    
    <VerticalStackLayout Margin="10" WidthRequest="300" HeightRequest="180" VerticalOptions="Center" HorizontalOptions="Center">
        <Label 
            Text="STATISTICS"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            TextColor="Black"
            FontSize="Small"
            FontFamily="DroidSerifRegular"
            Margin="0, 30, 0, 0"
            FontAttributes="Bold"
            />
        <HorizontalStackLayout HorizontalOptions="Center">
            <Label
                Text="{Binding NumWins}"
                FontSize="Header"
                FontFamily="DroidSerifRegular"
                TextColor="Black" 
                Margin="10, 10, 10, 0"
                />
            <Label
                Text="{Binding PercentWon}"
                FontSize="Header"
                FontFamily="DroidSerifRegular"
                TextColor="Black" 
                Margin="10, 10, 10, 0"
                />
            <Label
                Text="{Binding Streak}"
                FontSize="Header"
                FontFamily="DroidSerifRegular"
                TextColor="Black" 
                Margin="10, 10, 10, 0"
                />
        </HorizontalStackLayout>

        <HorizontalStackLayout HorizontalOptions="Center">
            <Label
                Text="Won"
                FontSize="Micro"
                FontFamily="DroidSerifRegular"
                TextColor="Black" 
                Margin="10, 0, 10, 20"
 
                />
            <Label
                Text="Win %"
                FontSize="Micro"
                FontFamily="DroidSerifRegular"
                TextColor="Black" 
                Margin="10, 0, 10, 20"
                />
            <Label
                Text="Streak"
                FontSize="Micro"
                FontFamily="DroidSerifRegular"
                TextColor="Black" 
                Margin="10, 0, 10, 20"
                />

        </HorizontalStackLayout>
    </VerticalStackLayout>
</mct:Popup>
    
    